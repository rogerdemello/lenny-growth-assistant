# Normalize line endings so the repo is identical regardless of the OS it was
# authored on. Without this, a Windows checkout produces a diff on every file.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
*.docx binary
*.mp4 binary
